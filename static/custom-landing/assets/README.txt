Place your static map screenshot here as map-screenshot.png.

Notes:
- Recommended size: ~1600×600 for best fit.
- The app will automatically show this image in the Map panel. If the file is missing, it will fall back to the inline SVG.
- Keep this directory’s assets local to preserve Runs on Atlassian eligibility (no external URLs).
